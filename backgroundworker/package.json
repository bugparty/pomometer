{
	"name": "backgroundworker",
	"type": "module",
	"scripts": {
		"dev": "pnpm dlx prisma generate --schema=../prisma/schema.prisma && wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"@prisma/adapter-d1": "^6.14.0",
		"@prisma/client": "^6.14.0",
		"hono": "^4.9.4"
	},
	"devDependencies": {
		"dotenv": "^17.2.1",
		"prisma": "^6.14.0",
		"wrangler": "^4.32.0"
	}
}